using Mapster;

namespace SnowyRiver.Accounts.Manager;

public static class TypeAdapterExtensions
{
    public static TypeAdapterConfig AddAccountsManager(this TypeAdapterConfig config)
    {

        return config;
    }
}
