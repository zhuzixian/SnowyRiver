<UserControl x:Class="SnowyRiver.WPF.Modules.Splash.Views.DialogView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:prism="http://prismlibrary.com/"
             xmlns:controls="clr-namespace:SnowyRiver.WPF.MaterialDesignInPrism.Controls;assembly=SnowyRiver.WPF.MaterialDesignInPrism"
             prism:ViewModelLocator.AutoWireViewModel="True">
    <Grid>
        <controls:TripsDialog Title="{Binding Title}"
                              Message="{Binding Message}"
                              Buttons="{Binding Buttons}"
                              Command="{Binding Command}"/>
    </Grid>
</UserControl>
