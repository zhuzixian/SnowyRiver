<ResourceDictionary xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation" 
                    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">
    <Style
        x:Key="SnowyRiver.Styles.Dialog.Title"
        BasedOn="{StaticResource MaterialDesignHeadline6TextBlock}"
        TargetType="TextBlock">
        <Setter Property="Margin" Value="0,0,0,12" />
        <Setter Property="HorizontalAlignment" Value="Center" />
        <Setter Property="Foreground" Value="{DynamicResource MaterialDesign.Brush.Primary}" />
    </Style>

    <Style
        x:Key="SnowyRiver.Styles.Dialog.Button"
        BasedOn="{StaticResource MaterialDesignFlatMidBgButton}"
        TargetType="Button">
        <Setter Property="Margin" Value="12,0" />
        <Setter Property="Width" Value="100" />
        <Setter Property="Height" Value="42" />
        <Setter Property="Foreground" Value="{DynamicResource MaterialDesign.Brush.Primary.Dark.Foreground}" />
    </Style>
</ResourceDictionary>